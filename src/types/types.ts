export type UserSubmitForm = {
    fullName: string;
    email: string;
    password: string;
    userType: string;
  };

  
export type LoginSubmitForm = {
    email: string;
    password: string;
  };